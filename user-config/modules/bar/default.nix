# All bar leaves (waybar / sketchybar / ags) are opt-in. Hosts that want a
# bar import the relevant leaf directly. This bundle is empty by design -- it
# exists so hosts can keep a uniform `./bar` import line without choosing a
# bar implementation, and so the per-bar `./waybar`, `./sketchybar`, `./ags`
# paths stay stable.
{ }
